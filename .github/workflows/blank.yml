name: lesson

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  myjob:
    runs-on: ubuntu-latest

    steps:
      - id: first
        run: echo "test=hello\nworld" >> $GITHUB_OUTPUT
      - run: echo ${{ steps.first.outputs.test }}